# Spring properties
spring:
  application:
     name: producer-service
  cloud:
    config:
      uri: http://configsrv.westus.cloudapp.azure.com:8888
# HTTP Server
server:
  port: 2211   # HTTP (Tomcat) ports